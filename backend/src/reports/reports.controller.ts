import { Controller, Get, Param, Post, UseGuards } from '@nestjs/common';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard'; import { PermissionsGuard } from '../auth/guards/permissions.guard'; import { Permissions } from '../auth/permissions.decorator'; import { AuthenticatedUser, CurrentUser } from '../auth/current-user.decorator'; import { ReportsService } from './reports.service';
@UseGuards(JwtAuthGuard,PermissionsGuard) @Controller('reports') export class ReportsController { constructor(private readonly service:ReportsService){} @Permissions('documents.read') @Get() list(@CurrentUser()u:AuthenticatedUser){return this.service.list(u.organizationId)} @Permissions('documents.generate') @Post('audit-periods/:id/prepare') prepare(@CurrentUser()u:AuthenticatedUser,@Param('id')id:string){return this.service.prepare(u.organizationId,u.id,id)} }
