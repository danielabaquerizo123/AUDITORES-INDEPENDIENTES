# Despliegue
Desarrollo local usa Docker Compose para PostgreSQL y Redis enlazados a loopback. Railway y producción: PENDIENTE DE VALIDACIÓN FUNCIONAL. Secretos se inyectan por entorno, nunca por Git.
