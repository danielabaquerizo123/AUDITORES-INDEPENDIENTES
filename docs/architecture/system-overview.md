# Visión del sistema
Monorepo con `backend` NestJS, `frontend` React/Vite, PostgreSQL, Redis/BullMQ y almacenamiento local excluido de Git. Los dominios se separan en módulos. PENDIENTE DE VALIDACIÓN FUNCIONAL: modelo y flujos de auditoría definitivos.
