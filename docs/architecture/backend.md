# Backend
NestJS expone `/api`, OpenAPI en `/api/docs` y salud en `/api/health`. Configuración validada, Helmet, CORS configurable, rate limiting, DTOs y filtro de excepciones son obligatorios. Prisma solo se ejecuta en backend.
