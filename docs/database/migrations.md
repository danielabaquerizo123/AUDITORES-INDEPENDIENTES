# Migraciones
Toda modificación aprobada se crea como migración Prisma revisada. No se ejecutan migraciones definitivas de negocio en esta fase. Validar con `npm --workspace backend run prisma:validate`.
