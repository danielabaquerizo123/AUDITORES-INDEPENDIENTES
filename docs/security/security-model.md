# Modelo de seguridad
Backend aplica Helmet, CORS configurable, validación estricta, rate limiting y errores públicos seguros. Argon2id y JWT están preparados. Los uploads requerirán validación de tipo, tamaño y almacenamiento seguro antes de implementarse.
