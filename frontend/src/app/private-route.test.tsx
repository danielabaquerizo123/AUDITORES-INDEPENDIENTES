import { MemoryRouter, Route, Routes } from 'react-router-dom'; import { render, screen } from '@testing-library/react'; import { describe, expect, it, vi } from 'vitest'; import { PrivateRoute } from './private-route';
const auth=vi.fn();vi.mock('./use-auth',()=>({useAuth:()=>auth()}));vi.mock('../services/session',()=>({session:{get token(){return auth()?.isAuthenticated?'token':null}}}));
describe('PrivateRoute',()=>{it('redirects anonymous users',()=>{auth.mockReturnValue({isAuthenticated:false});render(<MemoryRouter initialEntries={['/dashboard']}><Routes><Route element={<PrivateRoute/>}><Route path="/dashboard" element={<p>private</p>}/></Route><Route path="/login" element={<p>login</p>}/></Routes></MemoryRouter>);expect(screen.getByText('login')).toBeInTheDocument()});it('renders authenticated routes',()=>{auth.mockReturnValue({isAuthenticated:true});render(<MemoryRouter initialEntries={['/dashboard']}><Routes><Route element={<PrivateRoute/>}><Route path="/dashboard" element={<p>private</p>}/></Route></Routes></MemoryRouter>);expect(screen.getByText('private')).toBeInTheDocument()})});
