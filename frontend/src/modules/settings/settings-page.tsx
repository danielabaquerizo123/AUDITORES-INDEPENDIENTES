import { useState } from 'react';
import { useAuth } from '../../app/use-auth';
import '../../styles/settings.css';
function Eye({open}:{open:boolean}){return <span aria-hidden="true">{open?'◉':'◌'}</span>}
function PasswordField({label,value,onChange}:{label:string;value:string;onChange:(value:string)=>void}){const [shown,setShown]=useState(false);return <label className="settings-field"><span>{label}</span><div><input type={shown?'text':'password'} value={value} onChange={e=>onChange(e.target.value)} autoComplete="new-password"/><button type="button" aria-label={shown?'Ocultar contraseña':'Mostrar contraseña'} onClick={()=>setShown(!shown)}><Eye open={shown}/></button></div></label>}
export function SettingsPage(){const {user}=useAuth();const fullName=[user?.firstName,user?.lastName].filter(Boolean).join(' ')||'Usuario';const [editing,setEditing]=useState(false);const [name,setName]=useState(fullName);const [current,setCurrent]=useState(''),[password,setPassword]=useState(''),[confirmation,setConfirmation]=useState('');const checks=[password.length>=8,/[A-Z]/.test(password)&&/[a-z]/.test(password),/\d/.test(password),/[^A-Za-z0-9]/.test(password)];const valid=checks.every(Boolean)&&password===confirmation&&!!current;return <section className="settings-page" aria-label="Configuración"><header><h1>Configuración</h1><p>Administra los datos de acceso y la seguridad de tu cuenta.</p></header><article className="settings-card settings-user"><div className="settings-icon">♙</div><div className="settings-card-content"><h2>Datos de usuario</h2><p>Puedes actualizar el nombre de usuario con el que accedes al sistema.</p><div className="settings-user-row"><label className="settings-field"><span>Nombre de usuario</span><input value={name} disabled={!editing} onChange={e=>setName(e.target.value)}/></label>{editing?<div className="settings-buttons"><button className="settings-secondary" onClick={()=>{setName(fullName);setEditing(false)}}>Cancelar</button><button className="settings-primary" onClick={()=>setEditing(false)}>Guardar cambios</button></div>:<button className="settings-secondary" onClick={()=>setEditing(true)}>✎ Editar usuario</button>}</div></div></article><article className="settings-card"><div className="settings-icon">▣</div><div className="settings-card-content"><h2>Cambiar contraseña</h2><p>Por seguridad, utiliza una contraseña segura y actualízala periódicamente.</p><div className="settings-password-grid"><div><PasswordField label="Contraseña actual" value={current} onChange={setCurrent}/><PasswordField label="Nueva contraseña" value={password} onChange={setPassword}/><PasswordField label="Confirmar nueva contraseña" value={confirmation} onChange={setConfirmation}/></div><aside className="settings-requirements"><h3>◈ Requisitos de la contraseña</h3>{['Al menos 8 caracteres','Incluir letras mayúsculas y minúsculas','Incluir al menos un número','Incluir al menos un carácter especial'].map((text,i)=><p key={text} className={checks[i]?'met':''}><i>{checks[i]?'✓':'○'}</i>{text}</p>)}<button className="settings-primary" disabled={!valid}>▣ Actualizar contraseña</button></aside></div></div></article><article className="settings-card settings-security"><div className="settings-icon security">✓</div><div className="settings-card-content"><h2>Seguridad de la cuenta</h2><p>Por seguridad, utiliza una contraseña que no emplees en otros servicios.</p></div></article></section>}
